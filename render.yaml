services:
  - type: web
    name: wall-segment-server
    env: python
    buildCommand: ""
    startCommand: "uvicorn server:app --host 0.0.0.0 --port 8000"
    plan: free
    branch: main
    repo: https://github.com/YOUR_GITHUB_REPO_HERE
